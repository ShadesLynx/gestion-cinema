# Use Symfony's default session-based CSRF protection.
# This avoids double-submit cookie requirements during local development.
framework:
    form:
        csrf_protection: true

    csrf_protection: true
